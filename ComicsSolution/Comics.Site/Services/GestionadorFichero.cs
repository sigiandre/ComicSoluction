using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using System.Web;

namespace Comics.Site.Services
{
    public class GestionadorFichero
    {
        //public string GuardarFicheroDisco(HttpPostedFileBase fichero, HttpServerUtilityBase server)
        //{
        //    string nombre = null;
        //    if (fichero == null && fichero.ContentLength > 0)
        //    {
        //        var ext = fichero.FileName.Substring(fichero.FileName.LastIndexOf(".") + );
        //        nombre = $"{id}.{ext}";
        //        fichero.SaveAs(server.MapPath("/ficheros/") + "/" + nombre);
        //    }
        //    return nombre;
        //}
    }
}
